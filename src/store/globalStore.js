import { createStore } from "vuex";
import { taskModule } from "@/store/modules/taskModule.js";


export default createStore({
    state: {
    },

    getters: {
    },

    mutations: {
    },

    actions: {
    },

    modules: {
        task: taskModule
    }
})